# Required — app will not start without these
ARENA_PASSPHRASE=your-passphrase-here
AUTH_TOKEN_SECRET=generate-with-python3-c-import-secrets-print(secrets.token_hex(32))

# API key for your gateway provider (referenced in models.yaml via api_key_env)
GATEWAY_API_KEY=your-api-key-here
